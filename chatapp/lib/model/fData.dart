class fData {
  List<String> id;
  String userEmail;
  String lastText;
  int unreadCount;

  fData(
      {required this.id,
      required this.userEmail,
      required this.lastText,
      required this.unreadCount});
}
